{
  "name": "bulkit-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build needed for static files'",
    "start": "npx serve -s . -l $PORT"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
